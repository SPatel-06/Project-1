MSE 131 Final — App Launch CPM Model

Files:
- cpm_calculator.py  → baseline model + 5 extensions
- experiments.py     → scenario comparison, sensitivity analysis, charts

Requirements:
- Python 3.x
- numpy, matplotlib (pip install numpy matplotlib)

To run:
- python cpm_calculator.py   → runs the full model
- python experiments.py      → runs experiments and generates charts